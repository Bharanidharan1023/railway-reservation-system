spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Railway_Reservation_System
    username: Bharanidharan
    password: Bharani@1
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update # Options: create, create-drop, update, validate, none
    show-sql: true # Log SQL queries in the console
    properties:
      hibernate:
        format_sql: true # Pretty print SQL logs

adminservice:
  min: 10